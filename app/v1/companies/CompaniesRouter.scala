package v1.companies

import javax.inject.Inject
import play.api.routing.Router.Routes
import play.api.routing.SimpleRouter
import play.api.routing.sird._

/**
  * Routes and URLs to the CompaniesResource controller.
  */
//class CompaniesRouter @Inject()(controller: CompaniesController) extends SimpleRouter {
//  val prefix = "/v1/companies"


//  override def routes: Routes = {
//    case GET(p"/$company_name") =>
//      controller.show(company_name)
//  }

//}
